# Правки v5 — меню, доп. лендинги, настройки, мобильная версия

**Дата:** 2026-06-14
**Статус:** в работе
**Контекст:** Симона занята (покупает домен, выгружает тексты из Таплинка). Просьба — двигать приложение без новых данных: улучшить личный кабинет, починить шестерёнку remove.bg, сделать меню и 3 доп. лендинга (личность / интенсив / консультация — пока заглушки с фирменным дизайном, тексты заменит позже), завести страницу настроек, продумать мобильную версию.

## Фазы

- [x] **Фаза 1 — шестерёнка remove.bg только в режиме редактора.** Стейт `showSettings`→`showKeyPanel` (панель ключа). Кнопка ключа теперь 🔑 (title «API-ключ remove.bg») и видна только при `isAdmin() && editorMode`. ⚙ освобождена под пользовательские настройки. Проверено в preview: при выключенном редакторе 🔑 нет, при включённом — появляется.
- [x] **Фаза 2 — страница «Настройки» (`SettingsScreen`).** Полноэкранная (`.lk-page`). Разделы: Аккаунт (имя/телефон → `updateUserProfile`, e-mail только чтение), Безопасность (смена пароля → `setAccountPassword` + `PasswordStrength`, валидация ≥8 и score≥2 и совпадение), Уведомления (тогл «Новости» → `user_metadata.notify_news`; «Важные» — всегда вкл, disabled), Конфиденциальность (4 документа + «Запросить удаление аккаунта» mailto), О приложении (`APP_VERSION`, поддержка, реквизиты), Выход. Вход: ⚙ в топбаре каталога (для всех) и ⚙ в топбаре кабинета. Проверено в preview (десктоп + 375px).
- [x] **Фаза 3 — кабинет: точка входа в настройки.** ⚙ (`.lk-top-ico`) в `lk-topbar` кабинета → `onOpenSettings`. Настройки рендерятся поверх кабинета (тот же z-index, позже в DOM); «← Назад» возвращает в кабинет. Проверено.
- [x] **Фаза 4 — меню + 3 лендинга.** Хеш-роутинг в `Landing` (`pageFromHash`, `#/`, `#/about`, `#/intensive`, `#/consult`, слушатель `hashchange`, скролл наверх при смене). Навбар: бренд (→ домой), пункты (О Симоне, Интенсив, Консультация, Тарифы — «Тарифы» уводит на главную и скроллит), Войти + Попробовать; активный пункт подсвечен; на мобайле (≤880px) — бургер-дровер (CSS `.lp-nav-links`/`.lp-burger`). Страницы `AboutPage`/`IntensivePage`/`ConsultPage` на общих хелперах (`PageHero`/`Band`/`InfoCard`/`Steps`/`PageCTABand`/`DraftChip`); тексты — черновые заглушки, CTA на сервис (onRegister) и заявку (mailto). Футер общий, с колонкой навигации. Проверено: все 3 страницы рендерятся, подсветка и бургер работают.
- [x] **Фаза 5 — мобильная версия.** Топбар каталога: `flex-wrap`, скрыт «Привет», кнопки переносятся, без обрезки. Табы каталога — горизонтальный скролл. Лендинг: Hero-грид сворачивается в колонку (≤860px), убран горизонтальный overflow (`html, body { overflow-x: hidden }` — фон-цветы декоративны). Кабинет/настройки — `.set-grid` в одну колонку. Проверено на 375px: `scrollWidth == clientWidth`, бургер-дровер ок.

## Гейты

`npm run build` ✓ (118ms). `npm run lint` — без новых ошибок в новом коде. Проверка в preview (десктоп 1280 + мобайл 375): лендинги, меню, бургер, настройки, кабинет, гейтинг 🔑. Авторизованная часть проверена через временный обход входа (потом удалён, в коде не осталось).

## Итог

**Реализовано целиком (фазы 1–5).** Что получилось: 🔑-ключ remove.bg только в режиме редактора; отдельная страница «Настройки» (аккаунт/безопасность/уведомления/приватность/о приложении) — туда вернулись данные профиля и смена пароля, убранные из кабинета ради «воздуха»; верхнее меню с 3 доп. лендингами (О Симоне/Интенсив/Консультация) на хеш-роутинге + мобильный бургер; мобильная версия (стекинг Hero, перенос топбара, скролл табов, убран горизонтальный overflow).

**Что осталось Симоне/на будущее:** заменить черновые тексты лендингов материалами из Таплинка; когда появится система записи — заменить mailto-заявки на форму; реальная отправка рассылки по флагу `notify_news`; жёсткое серверное удаление аккаунта по запросу.
