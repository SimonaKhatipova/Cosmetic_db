# Инфраструктура и подключение сервисов

> **Это главный план-карта по всем внешним сервисам.** Один документ, чтобы не держать
> всё в голове. Когда что-то подключили — ставим `[x]` и пишем дату. Открываем его в начале
> каждой сессии про «подключения».

Дата старта: 2026-06-14. Бренд/домены: **Simona Beauty** — `simonabeauty.ru` (главный),
`simonabeauty.online` (резерв/редирект). Регистратор: REG.RU.

---

## Карта сервисов (что зачем нужно)

| Сервис | Зачем | Статус |
|---|---|---|
| GitHub | Код, приватный репозиторий | ✅ есть |
| Netlify | Хостинг сайта (вместо GitHub Pages), env-переменные | ✅ аккаунт + связка с репо |
| Supabase | База данных, авторизация, серверные функции (оплаты) | ✅ работает |
| REG.RU | Домены `simonabeauty.ru` / `.online` + DNS | ✅ домены куплены |
| Самозанятость (НПД) | Юр.основание для приёма оплат | ✅ оформлено |
| Робокасса | Приём оплат подписки PRO (рекуррент) | 🟡 кабинет одобрен, не достроено |
| Яндекс 360 для бизнеса | Почта на домене (support@, hello@) | ⬜ не начато |
| Resend | Транзакционные письма (подтверждение, сброс пароля, OTP) | ⬜ не начато |
| Яндекс Метрика | Аналитика посещений | 🟡 аккаунт есть, счётчик не создан |
| Sentry | Мониторинг ошибок в проде | 🟡 аккаунт есть, проекта нет |
| remove.bg | Удаление фона у фото товаров | ✅ ключ есть |

**Жёсткая зависимость:** `домен → почта → транзакционные письма (Resend) → оплаты`.
Аналитику (Метрика) и мониторинг (Sentry) можно подключать параллельно в любой момент.

---

## Фаза 0. Что уже готово ✅

- [x] GitHub репозиторий
- [x] Netlify: аккаунт создан, связан с репозиторием
- [x] Supabase: БД, авторизация, edge-функции робокассы задеплоены
- [x] Домены `simonabeauty.ru` и `simonabeauty.online` куплены на REG.RU
- [x] Статус самозанятого оформлен
- [x] Робокасса: документы одобрены, кабинет есть
- [x] Куки-плашка реализована в коде (`CookieConsent` в `App.jsx`) — простой вариант для РФ, достаточно
- [x] Sentry: организация `simonabeauty` создана
- [x] Яндекс Метрика: аккаунт создан

---

## Фаза 1. Хостинг → домен (технические правки + переезд на Netlify)

Цель: сайт открывается на `https://simonabeauty.ru`, едет с Netlify.

> ⚠️ Момент переключения (cutover): когда меняем `base` на `/`, текущий сайт на GitHub Pages
> (`/Cosmetic_db/`) перестанет работать. Поэтому делаем правки → деплоим на Netlify → проверяем
> на netlify-адресе → только потом переключаем домен. Старый GitHub Pages можно потом отключить.

**Делает Клод (код):**
- [x] `vite.config.js`: `base` с `/Cosmetic_db/` на `/` (2026-06-14)
- [x] Поправить захардкоженные пути `/Cosmetic_db/` в `public/manifest.json` → `/` (2026-06-14)
- [x] Добавить `netlify.toml` (команда сборки + SPA-redirect на `index.html`) (2026-06-14)
- [x] Проверить сборкой: `npm run build` ОК, пути в `dist/index.html` корневые (2026-06-14)
- [x] Закоммитить + запушить в `test/full-app` (коммит `6f92f1e`, 2026-06-14)

> ⚠️ Найдено: Netlify сейчас собирает ветку `gh-pages` (старые скомпилированные файлы),
> а не исходники. Настоящий код — в `test/full-app` (`main` устарела). Решено собирать из
> исходников. MCP не умеет менять продакшн-ветку → делаем в интерфейсе Netlify (шаг ниже).

**Делает Симона (Netlify — переключить на сборку из исходников):**
- [ ] Netlify → Project configuration → Build & deploy → Continuous deployment →
      Branches and deploy contexts → **Production branch: `gh-pages` → `test/full-app`** → Save
- [ ] Build settings подтянутся из `netlify.toml` (build `npm run build`, publish `dist`) — проверить
- [ ] Trigger deploy (Deploys → Trigger deploy → Deploy site), дождаться «Published»
- [ ] Открыть `https://meek-platypus-244509.netlify.app` — сайт грузится, ассеты на месте

**Делает Симона (кабинеты — домен, ПОСЛЕ успешной сборки):**
- [ ] Netlify → Domain settings → добавить `simonabeauty.ru`
- [ ] REG.RU → DNS: прописать записи, которые выдаст Netlify (A-запись на Netlify + CNAME для www),
      ИЛИ переключить NS на Netlify DNS (Клод подскажет точные значения)
- [ ] `simonabeauty.online` → редирект на `.ru`
- [ ] Дождаться выпуска HTTPS-сертификата (Netlify делает сам, Let's Encrypt)
- [ ] Проверить, что сайт открывается на домене

---

## Фаза 2. Почта на домене (Яндекс 360 для бизнеса) — бесплатно

Цель: рабочие ящики `support@simonabeauty.ru`, `hello@simonabeauty.ru`.
Используем Яндекс, т.к. ты уже в его экосистеме, и это бесплатно.

**Делает Симона:**
- [ ] Завести организацию в Яндекс 360 для бизнеса, добавить домен `simonabeauty.ru`
- [ ] Подтвердить владение доменом (TXT-запись на REG.RU — Клод подскажет)
- [ ] Прописать MX-записи Яндекса на REG.RU (Клод подскажет точные значения)
- [ ] Создать ящики `support@` и `hello@`
- [ ] Вынести контакт (email + Telegram) на сайт

---

## Фаза 3. Транзакционные письма (Resend → SMTP в Supabase) — бесплатно

Цель: письма от приложения (подтверждение почты, сброс пароля, OTP) реально доходят.
Встроенная почта Supabase для прода не годится (жёсткий лимит писем) — нужен внешний SMTP.
Берём **Resend** (бесплатно 3000 писем/мес). Настройка только в кабинетах, MCP не требуется.

**Делает Симона (с подсказками Клода):**
- [ ] Resend: зарегистрироваться, добавить домен `simonabeauty.ru`
- [ ] Прописать на REG.RU SPF / DKIM / DMARC записи, которые выдаст Resend
- [ ] Дождаться статуса «Verified» в Resend
- [ ] Создать API-ключ / SMTP-доступ в Resend
- [ ] Supabase → Authentication → SMTP Settings: вставить host/port/login/pass от Resend,
      отправитель `noreply@simonabeauty.ru`
- [ ] Тест: регистрация нового пользователя → письмо приходит; сброс пароля → приходит

---

## Фаза 4. Оплаты (Робокасса) — достроить

Подписка PRO (3490 ₽/мес, автосписание) уже закодирована: edge-функции `robokassa-pay`,
`robokassa-result`, `robokassa-recurring-charge` + таблицы `payments`/`subscriptions`.
Робокасса **поддерживает рекуррент** — менять провайдера не нужно. Осталась настройка.
Детали: `plans/2026-06-13-robokassa-recurring.md`.

**Делает Симона (кабинеты):**
- [ ] Supabase → секреты функций: `ROBOKASSA_LOGIN/PASS1/PASS2/HASH/AMOUNT/TEST` + `CRON_SECRET`
- [ ] Кабинет Робокассы → Result URL / Success URL / Fail URL (Клод даст адреса функций)
- [ ] `ROBOKASSA_HASH` в секретах и в кабинете должны совпадать
- [ ] Включить рекуррентные платежи (может потребоваться заявка в поддержку Робокассы)
- [ ] Включить фискализацию (54-ФЗ) — обязательна
- [ ] Настроить cron (pg_cron) на ежедневный вызов `robokassa-recurring-charge`
- [ ] Тестовый платёж в тест-режиме (`ROBOKASSA_TEST=1`), затем боевой

---

## Фаза 5. Аналитика и мониторинг (можно параллельно)

**Яндекс Метрика:**
- [ ] В кабинете Метрики создать счётчик для `simonabeauty.ru`, скопировать номер/код
- [ ] Клод вставит код счётчика в `index.html`
- [ ] Проверить в Метрике, что визиты приходят

**Sentry:**
- [ ] В Sentry создать **проект** (платформа: React), получить DSN
- [ ] Клод добавит `@sentry/react` и инициализацию в код, DSN — в env Netlify
- [ ] (Опционально, позже) Settings → Integrations → GitHub: поставить Sentry GitHub App на репо
      (нужно для привязки ошибок к коммитам; не блокер)

---

## Фаза 6. Куки и правовое — почти готово

- [x] Куки-плашка есть (простой вариант «Принять» со ссылкой на Политику) — для РФ достаточно
- [ ] (Опционально, позже) Добавить кнопку «Отклонить» / страницу настроек куки — только если
      пойдём на ЕС-аудиторию. Сложный вариант с категориями (как у Cloudflare) сейчас НЕ нужен.

---

## Фаза 7. 152-ФЗ — перед публичным запуском (отложено)

- [ ] Перенести хранилище персональных данных в РФ-облако (Yandex Cloud / VK Cloud / Selectel)
- [ ] Подать уведомление оператора ПД в Роскомнадзор

---

## Итог

Реализован: ❌ нет (старт, идём по фазам).
Сделано к 2026-06-14: Фаза 0 (готовое зафиксировано), куки закрыты.
Следующий шаг: **Фаза 1** — технические правки под Netlify + домен.
