# Правки v4 — батч UX/тексты/сравнение/дубликаты/перфоманс

**Дата:** 2026-06-13
**Статус:** батч фиксов реализован; кабинет и заблокированные пункты — отдельно
**Контекст:** список правок от Симоны после прогона тестировщиками. Крупные фичи (личный кабинет как страница, модерация пользовательских средств) вынесены в свои планы/память: [[lk-vision-2026-06]], [[user-submitted-verification]].

## Фазы

- [x] **Лендинг.** Кнопка «Войти» (Navbar + Footer) — заметная: акцентная обводка/заливка. «Почему так происходит»: «закрыть эти боли» → «помочь разобраться с каждой из этих ситуаций».
- [x] **Маски ввода.** `sanitizeEmail` (только латиница + @._%+-), `sanitizePhone` (только цифры и один «+» в начале). Применены в RegisterScreen, LoginScreen, PurchaseModal. Проверено в preview: кириллица вырезается.
- [x] **Тексты.** «Код не подошёл…» → «Код не совпал. Проверьте все 6 цифр из письма или запросите новый». «7 поисков по базе средств в неделю» → «…по каталогу средств…» (везде). Метка кабинета «Поиск по базе» → «Поиск по каталогу».
- [x] **Сравнение.** Запрет сравнивать разные категории (`product_type`): guard в `addProductToCompare` + дизейбл кнопки и пояснение в ProductModal (`compareBaseType`). Строка «Заметка» → «Комментарий».
- [x] **Список/карточки.** Подгруппы в списке ингредиентов крупнее (13→14.5, bold). Фон карточек средств (`.card`, `.card-media`) осветлён.
- [x] **Закрытие окон.** Глобальный Esc закрывает верхнее окно. Экраны входа/регистрации: закрываются по клику в любую зону вне карточки (`!e.target.closest('.login3-card')` — раньше зона маскота/`login3-stage` над карточкой перехватывала клик и не закрывала). Проверено: клик над карточкой → лендинг; клик внутри карточки → остаётся открыт; Esc → лендинг.
- [x] **Согласия при регистрации.** Логика уже верная (Оферта + ПД/Политика обязательны, рассылка — нет; проверено: без галочек не пускает). Добавлены видимые маркеры: «обязательно» (красноватый) у Оферты и ПД/Политики, «по желанию» (серый) у рассылки.
- [x] **Дубликаты.** `findDuplicateProducts` (название + бренд, нормализация, Левенштейн). В AddProductModal при сохранении показывает похожие и спрашивает «Всё равно добавить?» — предупреждаем, но разрешаем. Сверка по составу — позже, вместе с модерацией.
- [x] **Перфоманс.** Первая страница средств 200→60, ингредиентов 300→90 (быстрый первый экран). Скелетоны `SkeletonGrid`/`SkeletonRows` вместо пустого экрана; `LoadingMascot` переиспользован на экране проверки сессии.
- [x] **Пароль при регистрации.** Шаг 3 «Придумайте пароль» после OTP: `setAccountPassword` (PUT /auth/v1/user), компонент `PasswordStrength` (бар 0–4 + подсказки), валидация (≥8 и score≥2), кнопка «Пропустить». Решение Симоны: шаг после кода + смена в кабинете. Теперь вход по почте+паролю работает.

- [x] **Юр.документы на сайт.** 4 .docx → `public/legal/*.html` (mammoth + брендовый шаблон, сохранены нумерация/списки/ссылки). `legal.js` → `BASE_URL + legal/`. Убрана привязка к Яндекс.Диску. Проверено: 200, рендер ок.
- [x] **Регистрация v2.** Обязательные поля (имя/почта/телефон) и согласия (оферта, ПД) — красная звёздочка `*` вместо слова. Кнопка «Получить код» дизейблится, пока всё обязательное не заполнено/отмечено (телефон стал обязателен). Проверено в preview.
- [x] **Прогресс-бар.** Гусеница (MetamorphosisBar) → зелёные блоки-батарейка (HairRecoveryBar, как до змейки, коммит 3e3c3cc), заполняются слева направо. Мёртвый код удалён. Проверено.
- [x] **Лендинг «Почему так происходит».** Заголовок → «Три причины, почему уход не работает», переписаны интро и 3 карточки болей. Проверено.
- [x] **Login backdrop fix v2.** Закрытие по клику в любую зону вне карточки (`!closest('.login3-card')`) — раньше зона маскота перехватывала.
- [x] **Согласия — звёздочки** (см. «Регистрация v2»).

## Осталось

- [ ] **Лендинговые плашки Hero** — Симоне нужны идеи для конверсии (не менять код пока). Предложения даны в чате; ждём выбор.
- [ ] **Модерация пользовательских средств** — миграция БД+RLS, согласовать перед применением. См. [[user-submitted-verification]].

## Гейты

`npm run build` ✓ (116ms, без ошибок). Проверено в preview: кнопка «Войти», рефраза, маски почты/телефона, Esc-закрытие.

## Итог

Батч реализован и проверен. Крупные фичи (кабинет-страница, модерация) — следующими шагами, часть пунктов ждёт материалов Симоны.
